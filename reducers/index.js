const initialState = {
  playerHand: '',
  cpuHand: '',
  playerScore: '',
  cpuScore: ''
}

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}
